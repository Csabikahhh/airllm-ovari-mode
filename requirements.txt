# Core runtime  (floors synced to the tested venv — see CLAUDE.md)
torch>=2.11
transformers>=5.10,<6
accelerate>=1.13,<2
safetensors>=0.7
huggingface-hub>=1.18
optimum>=2.1
tqdm>=4.68
sentencepiece>=0.2
einops>=0.8
psutil>=7

# Optional GPU compression path. On Windows/CUDA this may require a matching wheel;
# install separately if pip cannot resolve one for your Python/CUDA version:
#   pip install bitsandbytes
#
# Optional macOS Apple Silicon fallback path for upstream AirLLM/MLX:
#   pip install mlx
