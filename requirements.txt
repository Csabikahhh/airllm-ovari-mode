# Core runtime
torch>=2.5
transformers>=4.45,<6
accelerate>=1.0,<2
safetensors>=0.4
huggingface-hub>=0.26
optimum>=1.23
tqdm>=4.66
sentencepiece>=0.2
einops>=0.8
psutil>=6

# Optional GPU compression path. On Windows/CUDA this may require a matching wheel;
# install separately if pip cannot resolve one for your Python/CUDA version:
#   pip install bitsandbytes

# Development / evaluation helpers kept from the original project.
evaluate>=0.4
scikit-learn>=1.5
wandb>=0.17
